spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
    favicon:
      enabled: true

  datasource:
    username: root
    password: admin123
    url: jdbc:mysql://localhost:3306/javaeestore?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
    mapper-locations: classpath:mybatis/mapper/*.xml
    type-aliases-package: com.sustart.Model
